import { defineConfig } from 'vite';

export default defineConfig({
  root: '.', // Diretório raiz do seu projeto (onde está o index.html)
  build: {
    outDir: 'dist', // Diretório de saída para os arquivos compilados
  },
});


  



